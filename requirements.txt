# Security Management Platform V9.0.3 — Python Dependencies
# =========================================================
# Core
APScheduler>=3.10.0
requests>=2.31.0
cryptography>=41.0.5,<42.1
pyee>=11.0.0,<12.0.0
urllib3>=1.25.8,<2.0.0
protobuf>=3.14,<5.0.0

# Reporting
reportlab>=4.0.0
Jinja2>=3.1.2
weasyprint>=60.2

# Security scanning
sslyze>=5.2.0
python-owasp-zap-v2.4>=0.0.21
theHarvester

# ── V9.0.3 New Dependencies ──────────────────────────────────────────────────────
# API (Secured REST API with JWT and rate limiting)
fastapi>=0.111.0
uvicorn>=0.30.0
python-jose[cryptography]>=3.3.0
slowapi>=0.1.9
pydantic>=2.0.0

# SBOM Generation (CycloneDX — fallback to SPDX/CSV if not available)
cyclonedx-python-lib>=4.0.0

# Screenshot Evidence (optional — graceful fallback to HTML snapshot if not installed)
playwright>=1.40.0

# System resource monitoring (optional — graceful fallback to /proc if not installed)
psutil>=5.9.0

# ── Missing Third Party Dependencies ───────────────────────────────────────────
PySide6>=6.0.0
python-dateutil>=2.8.2
colorama>=0.4.6
netaddr>=1.3.0
aiomultiprocess>=0.9.0
pysqlcipher3>=1.2.0
shodan>=1.31.0
aiohttp-socks>=0.8.4
pycryptodomex>=3.19.0
certifi>=2023.11.17
ujson>=5.9.0
dnspython>=2.5.0
aiohttp>=3.9.1
aiodns>=3.1.1
uvloop>=0.19.0; sys_platform != 'win32'
PyYAML>=6.0.1
beautifulsoup4>=4.12.2
termcolor>=2.4.0
pytest>=8.0.0
ratelimit>=2.2.1
aiosqlite>=0.19.0
requests-futures>=1.0.1
httpx>=0.26.0
